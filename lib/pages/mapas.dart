import 'package:flutter/material.dart';
import 'package:scanner/widgets/scan_tiles.dart';

class MapasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => ScanTiles(tipo: 'geo');
}
